# 🫁 مشروع اكتشاف الالتهاب الرئوي من صور الأشعة السينية

<div align="center">

![Python](https://img.shields.io/badge/Python-3.12-blue)
![TensorFlow](https://img.shields.io/badge/TensorFlow-2.x-orange)
![FastAPI](https://img.shields.io/badge/FastAPI-Latest-green)
![License](https://img.shields.io/badge/License-MIT-yellow)

**نظام ذكاء اصطناعي متقدم لاكتشاف الالتهاب الرئوي من صور الأشعة السينية للصدر**

</div>

---

## 📖 جدول المحتويات

1. [ما هذا المشروع؟](#-ما-هذا-المشروع)
2. [كيف يعمل الذكاء الاصطناعي؟](#-كيف-يعمل-الذكاء-الاصطناعي)
3. [هيكل المشروع](#-هيكل-المشروع)
4. [المتطلبات والتثبيت](#-المتطلبات-والتثبيت)
5. [شرح الملفات بالتفصيل](#-شرح-الملفات-بالتفصيل)
6. [كيفية تدريب النموذج](#-كيفية-تدريب-النموذج)
7. [كيفية تشغيل الـ API](#-كيفية-تشغيل-الـ-api)
8. [كيفية اختبار النموذج](#-كيفية-اختبار-النموذج)
9. [شرح الكود خطوة بخطوة](#-شرح-الكود-خطوة-بخطوة)
10. [الأسئلة الشائعة](#-الأسئلة-الشائعة)

---

## 🤔 ما هذا المشروع؟

### الفكرة البسيطة

تخيل أنك طبيب وأمامك مئات صور الأشعة السينية للصدر، وتريد معرفة أي منها يُظهر التهاب رئوي وأي منها سليم. هذا المشروع يقوم بهذا العمل **تلقائياً** باستخدام الذكاء الاصطناعي!

### ماذا يفعل المشروع؟

1. **يتعلم** من آلاف صور الأشعة السينية (صور سليمة وصور بها التهاب رئوي)
2. **يحفظ** ما تعلمه في ملف يسمى "النموذج" (Model)
3. **يستخدم** ما تعلمه لفحص صور جديدة وتحديد إذا كانت تُظهر التهاب رئوي أم لا

### مثال عملي

```
📷 تعطي الكمبيوتر صورة أشعة سينية
    ⬇️
🧠 الذكاء الاصطناعي يفحص الصورة
    ⬇️
📊 النتيجة: "PNEUMONIA" (التهاب رئوي) بنسبة ثقة 95%
   أو: "NORMAL" (سليم) بنسبة ثقة 98%
```

---

## 🧠 كيف يعمل الذكاء الاصطناعي؟

### 1. الشبكة العصبية (Neural Network)

الذكاء الاصطناعي في هذا المشروع يستخدم شيئاً يسمى **الشبكة العصبية** وهي تحاكي طريقة عمل الدماغ البشري!

```
📷 الصورة ← [طبقة 1] ← [طبقة 2] ← [طبقة 3] ← ... ← 🎯 النتيجة
                ↓           ↓           ↓
           يتعرف على    يتعرف على    يتعرف على
            الحواف       الأشكال      المعالم
```

### 2. EfficientNet - العقل المدبر

نحن نستخدم شبكة عصبية جاهزة ومتقدمة تسمى **EfficientNet**. تخيلها مثل طالب ذكي جداً درس ملايين الصور من قبل!

**لماذا EfficientNet؟**
- ✅ سريعة في التعلم
- ✅ دقيقة جداً
- ✅ لا تحتاج كمبيوتر خارق

### 3. التعلم بالنقل (Transfer Learning)

بدلاً من تعليم الكمبيوتر من الصفر، نأخذ شبكة عصبية تعلمت بالفعل كيف تتعرف على الصور، ونعلمها فقط الجزء الخاص بالأشعة السينية.

**مثال للتوضيح:**
```
🎓 شخص يعرف اللغة العربية (معرفة سابقة)
    ⬇️
📚 نعلمه المصطلحات الطبية فقط (معرفة جديدة)
    ⬇️
👨‍⚕️ الآن يفهم التقارير الطبية!
```

---

## 📁 هيكل المشروع

```
project/
├── 📂 chest_xray/              # مجلد البيانات (صور الأشعة)
│   ├── train/                  # صور التدريب
│   │   ├── NORMAL/            # صور سليمة
│   │   └── PNEUMONIA/         # صور بها التهاب رئوي
│   ├── val/                    # صور التحقق
│   └── test/                   # صور الاختبار
│
├── 📂 results/                 # النتائج والنماذج المحفوظة
│   ├── models/                # النماذج المدربة
│   │   ├── model_final.keras  # النموذج النهائي
│   │   └── model_stage1_frozen.keras
│   ├── plots/                 # الرسوم البيانية
│   └── training_config.json   # إعدادات التدريب
│
├── 📂 logs/                    # سجلات التدريب
├── 📂 venv/                    # البيئة الافتراضية
│
├── 📄 model.py                 # ⭐ الملف الرئيسي للتدريب
├── 📄 api.py                   # ⭐ واجهة برمجة التطبيقات
├── 📄 config.py                # ⭐ إعدادات المشروع
├── 📄 evaluate_model.py        # تقييم النموذج
├── 📄 simple_predict.py        # تجربة النموذج
├── 📄 test_api.py              # اختبار الـ API
├── 📄 requirements.txt         # المكتبات المطلوبة
├── 📄 Dockerfile               # للتشغيل في Docker
└── 📄 README.md                # هذا الملف!
```

---

## 💻 المتطلبات والتثبيت

### المتطلبات الأساسية

| المتطلب | الإصدار | الغرض |
|---------|---------|-------|
| Python | 3.10+ | لغة البرمجة |
| TensorFlow | 2.x | مكتبة الذكاء الاصطناعي |
| FastAPI | Latest | واجهة الـ API |
| Pillow | Latest | معالجة الصور |

### خطوات التثبيت

#### الخطوة 1: إنشاء البيئة الافتراضية
```bash
# إنشاء البيئة
python3 -m venv venv

# تفعيل البيئة (Linux/Mac)
source venv/bin/activate

# تفعيل البيئة (Windows)
venv\Scripts\activate
```

#### الخطوة 2: تثبيت المكتبات
```bash
pip install -r requirements.txt
```

#### الخطوة 3: تحميل البيانات
البيانات عبارة عن صور أشعة سينية من Kaggle:
- رابط البيانات: [Chest X-Ray Images (Pneumonia)](https://www.kaggle.com/paultimothymooney/chest-xray-pneumonia)
- ضعها في مجلد `chest_xray/`

---

## 📝 شرح الملفات بالتفصيل

### 1. 🎯 model.py - ملف التدريب الرئيسي

هذا هو قلب المشروع! يحتوي على كل ما يتعلق بتدريب النموذج.

#### الأجزاء الرئيسية:

```python
# ========================
# 1. TrainingConfig - إعدادات التدريب
# ========================
@dataclass
class TrainingConfig:
    """كل الإعدادات في مكان واحد"""
    img_size: int = 224          # حجم الصورة (224×224 بكسل)
    batch_size: int = 16         # عدد الصور في كل دفعة
    initial_epochs: int = 30     # عدد مرات التدريب
    dropout_rate: float = 0.4    # نسبة منع الـ Overfitting
```

**ما هو Batch Size؟**
تخيل أن عندك 1000 صورة. بدلاً من تدريب الكمبيوتر على صورة واحدة في كل مرة، نعطيه 16 صورة معاً (دفعة واحدة) لتسريع التعلم.

```python
# ========================
# 2. DataManager - إدارة البيانات
# ========================
class DataManager:
    """يجهز الصور للتدريب"""
    
    def create_optimized_dataset(self, directory, shuffle):
        # يقرأ الصور من المجلد
        # يغير حجمها إلى 224×224
        # يحولها إلى أرقام (0-255)
        # يخلطها (للتدريب فقط)
```

```python
# ========================
# 3. ModelBuilder - بناء النموذج
# ========================
class ModelBuilder:
    """يبني الشبكة العصبية"""
    
    def build(self):
        # 1. نأخذ EfficientNet الجاهزة
        base_model = EfficientNetB0(weights='imagenet')
        
        # 2. نجمد طبقاتها (لا نغيرها)
        base_model.trainable = False
        
        # 3. نضيف طبقاتنا الخاصة
        x = GlobalAveragePooling2D()(base_model.output)
        x = Dense(256, activation='relu')(x)
        x = Dropout(0.4)(x)  # للوقاية من Overfitting
        output = Dense(1, activation='sigmoid')(x)  # النتيجة النهائية
```

**ما هو Overfitting؟**
عندما يحفظ النموذج صور التدريب بدلاً من أن يتعلم منها. مثل طالب يحفظ الأسئلة بدلاً من فهم المادة!

```python
# ========================
# 4. Trainer - التدريب
# ========================
class Trainer:
    """يدرب النموذج على الصور"""
    
    def train(self):
        # المرحلة 1: تدريب الطبقات الجديدة فقط
        model.fit(train_data, epochs=30)
        
        # المرحلة 2: Fine-tuning
        # نفك تجميد بعض طبقات EfficientNet
        # ونعيد التدريب بمعدل تعلم أقل
        base_model.trainable = True
        model.fit(train_data, epochs=25)
```

---

### 2. 🌐 api.py - واجهة برمجة التطبيقات (API)

هذا الملف يحول النموذج إلى خدمة ويب يمكن لأي برنامج استخدامها!

```python
# ========================
# الإعدادات
# ========================
from config import settings

# مسار النموذج: results/models/model_final.keras
# المنفذ: 8000
# العنوان: http://localhost:8000
```

```python
# ========================
# PneumoniaDetector - كاشف الالتهاب الرئوي
# ========================
class PneumoniaDetector:
    """يحمل النموذج ويستخدمه للتنبؤ"""
    
    def __init__(self, model_path):
        # يحمل النموذج من الملف
        self.model = keras.models.load_model(model_path)
        
        # يعرف حجم الصورة المطلوب
        self.img_size = 224
    
    def preprocess_image(self, image_bytes):
        """يجهز الصورة للتنبؤ"""
        # 1. يفتح الصورة
        image = Image.open(io.BytesIO(image_bytes))
        
        # 2. يحولها إلى RGB (ألوان)
        image = image.convert('RGB')
        
        # 3. يغير حجمها إلى 224×224
        image = image.resize((224, 224))
        
        # 4. يحولها إلى أرقام
        return np.array(image)
    
    def predict(self, image_bytes):
        """يتنبأ بالنتيجة"""
        # يجهز الصورة
        img = self.preprocess_image(image_bytes)
        
        # يحصل على التنبؤ (رقم بين 0 و 1)
        prediction = self.model.predict(img)[0][0]
        
        # إذا كان > 0.5 = PNEUMONIA
        # إذا كان < 0.5 = NORMAL
        if prediction >= 0.5:
            return "PNEUMONIA", prediction
        else:
            return "NORMAL", 1 - prediction
```

```python
# ========================
# نقاط النهاية (Endpoints)
# ========================

@app.get("/")
async def root():
    """الصفحة الرئيسية"""
    return {"status": "running"}

@app.get("/health")
async def health_check():
    """فحص صحة الخدمة"""
    return {"status": "healthy", "model_loaded": True}

@app.post("/predict")
async def predict_image(file: UploadFile):
    """
    نقطة التنبؤ الرئيسية
    
    الاستخدام:
    1. ارفع صورة أشعة سينية
    2. احصل على النتيجة
    
    مثال على النتيجة:
    {
        "prediction": "PNEUMONIA",
        "confidence": 0.95,
        "probabilities": {
            "NORMAL": 0.05,
            "PNEUMONIA": 0.95
        }
    }
    """
    image_bytes = await file.read()
    result = detector.predict(image_bytes)
    return result
```

---

### 3. ⚙️ config.py - إعدادات المشروع

```python
@dataclass
class Settings:
    """كل الإعدادات في مكان واحد"""
    
    # مسار النموذج
    MODEL_PATH = "results/models/model_final.keras"
    
    # مسار الإعدادات
    CONFIG_PATH = "results/training_config.json"
    
    # إعدادات الخادم
    HOST = "0.0.0.0"  # يسمح بالوصول من أي جهاز
    PORT = 8000        # رقم المنفذ
    
    # إعدادات الصورة
    IMG_SIZE = 224     # حجم الصورة
```

---

### 4. 📊 evaluate_model.py - تقييم النموذج

```python
class ModelEvaluator:
    """يختبر مدى دقة النموذج"""
    
    def evaluate(self):
        """
        يحسب المقاييس التالية:
        
        1. Accuracy (الدقة): نسبة التنبؤات الصحيحة
           مثال: 95% = من كل 100 صورة، 95 صحيحة
        
        2. Precision (الدقة الإيجابية): 
           من الذين قلنا عنهم PNEUMONIA، كم كان فعلاً؟
        
        3. Recall (الاستدعاء):
           من كل المرضى الحقيقيين، كم اكتشفنا؟
        
        4. F1-Score: متوسط Precision و Recall
        
        5. AUC-ROC: مقياس شامل للجودة
        """
```

**مصفوفة الارتباك (Confusion Matrix):**
```
                    التنبؤ
                NORMAL  PNEUMONIA
الحقيقة  NORMAL    ✅      ❌
         PNEUMONIA  ❌      ✅
         
✅ = صحيح
❌ = خطأ
```

---

## 🚀 كيفية تدريب النموذج

### الأمر البسيط
```bash
python model.py
```

### ماذا يحدث؟

```
📚 الخطوة 1: تحميل البيانات
   ├── قراءة صور التدريب (5000+ صورة)
   ├── قراءة صور التحقق
   └── قراءة صور الاختبار

🔧 الخطوة 2: تجهيز البيانات
   ├── تغيير حجم كل صورة إلى 224×224
   ├── تطبيق التحسينات (دوران، تقليب، إلخ)
   └── تقسيم إلى دفعات

🏗️ الخطوة 3: بناء النموذج
   ├── تحميل EfficientNet
   ├── إضافة الطبقات الخاصة
   └── تجهيز للتدريب

🎓 الخطوة 4: التدريب (المرحلة 1)
   ├── 30 جولة تدريب
   ├── تجميد EfficientNet
   └── تدريب الطبقات الجديدة فقط

🎯 الخطوة 5: Fine-Tuning (المرحلة 2)
   ├── 25 جولة إضافية
   ├── فك تجميد بعض الطبقات
   └── تدريب بمعدل تعلم أقل

💾 الخطوة 6: الحفظ
   ├── results/models/model_final.keras
   ├── results/training_config.json
   └── results/plots/*.png
```

---

## 🌐 كيفية تشغيل الـ API

### الخطوة 1: تشغيل الخادم
```bash
# باستخدام البيئة الافتراضية
./venv/bin/python api.py

# أو
python api.py
```

### الخطوة 2: رؤية النتيجة
```
============================================================
  🫁 PNEUMONIA DETECTION API
============================================================
  Server: http://0.0.0.0:8000
  API Docs: http://localhost:8000/docs
  Model: results/models/model_final.keras
============================================================
```

### الخطوة 3: تجربة الـ API

**طريقة 1: عبر المتصفح**
1. افتح: http://localhost:8000/docs
2. اضغط على `/predict`
3. ارفع صورة
4. شاهد النتيجة!

**طريقة 2: عبر الأوامر**
```bash
curl -X POST "http://localhost:8000/predict" \
     -F "file=@chest_xray.jpg"
```

**طريقة 3: عبر Python**
```python
import requests

# ارفع الصورة
with open("chest_xray.jpg", "rb") as f:
    response = requests.post(
        "http://localhost:8000/predict",
        files={"file": f}
    )

# شاهد النتيجة
print(response.json())
# Output: {"prediction": "PNEUMONIA", "confidence": 0.95}
```

---

## 🧪 كيفية اختبار النموذج

### اختبار سريع
```bash
python simple_predict.py --image path/to/image.jpg
```

### تقييم شامل
```bash
python evaluate_model.py
```

**النتيجة المتوقعة:**
```
============================================================
                    📊 EVALUATION RESULTS
============================================================

✅ Accuracy:   92.5%
📊 Precision:  91.2%
📊 Recall:     94.1%
📊 F1-Score:   92.6%
📊 AUC-ROC:    0.967

Confusion Matrix:
                NORMAL  PNEUMONIA
NORMAL           230        20
PNEUMONIA         17       345

============================================================
```

---

## 🔧 شرح الكود خطوة بخطوة

### كيف يتعلم النموذج؟

```python
# 1. تجهيز البيانات
train_ds = DataManager.create_optimized_dataset("chest_xray/train")

# 2. بناء النموذج
model = ModelBuilder().build()

# 3. تجميع النموذج (تحديد طريقة التعلم)
model.compile(
    optimizer=Adam(learning_rate=0.0005),  # مدى سرعة التعلم
    loss='binary_crossentropy',             # كيف نقيس الخطأ
    metrics=['accuracy']                    # ماذا نتابع
)

# 4. التدريب!
model.fit(
    train_ds,           # بيانات التدريب
    epochs=30,          # 30 جولة
    validation_data=val_ds  # بيانات التحقق
)
```

### ماذا يحدث في كل جولة (Epoch)؟

```
الجولة 1/30
├── 📦 دفعة 1: 16 صورة
│   ├── Forward Pass: حساب التنبؤ
│   ├── Loss: حساب الخطأ (مثلاً 0.8)
│   └── Backward Pass: تعديل الأوزان
│
├── 📦 دفعة 2: 16 صورة
│   └── نفس الخطوات...
│
├── ... (باقي الدفعات)
│
└── 📊 نهاية الجولة:
    ├── Training Accuracy: 75%
    ├── Validation Accuracy: 72%
    └── Loss: 0.5

الجولة 30/30
└── 📊 نهاية التدريب:
    ├── Training Accuracy: 95%
    ├── Validation Accuracy: 92%
    └── Loss: 0.15
```

---

## ❓ الأسئلة الشائعة

### Q: لماذا حجم الصورة 224×224؟
**A:** لأن EfficientNetB0 مصممة لهذا الحجم. أحجام أكبر = وقت أطول، أحجام أصغر = دقة أقل.

### Q: ما هو الـ Epoch؟
**A:** جولة واحدة على كل صور التدريب. إذا عندك 5000 صورة وعملت 30 epoch، فالنموذج رأى كل صورة 30 مرة!

### Q: لماذا نستخدم Validation Data؟
**A:** للتأكد أن النموذج يتعلم فعلاً وليس يحفظ! نختبره على صور لم يرها أثناء التدريب.

### Q: ما الفرق بين .keras و .h5؟
**A:** كلاهما صيغ لحفظ النموذج. `.keras` هي الصيغة الجديدة والأفضل في Keras 3.

### Q: لماذا يستخدم GPU بدلاً من CPU؟
**A:** GPU أسرع بـ 10-100 مرة! لكن المشروع يعمل على CPU أيضاً (أبطأ فقط).

### Q: كيف أحسن دقة النموذج؟
**A:** 
1. أضف المزيد من البيانات
2. جرب معدلات تعلم مختلفة
3. زد عدد الـ epochs
4. استخدم تحسينات (augmentation) مختلفة

---

## 📚 مصادر للتعلم

- [TensorFlow Documentation](https://www.tensorflow.org/)
- [Keras Documentation](https://keras.io/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Deep Learning Book](https://www.deeplearningbook.org/)

---

## 🤝 المساهمة

مرحباً بأي مساهمة! يمكنك:
1. الإبلاغ عن Bugs
2. اقتراح ميزات جديدة
3. تحسين الكود
4. تحسين التوثيق

---

## 📄 الرخصة

هذا المشروع مرخص تحت MIT License.

---

<div align="center">

**صُنع بـ ❤️ لمساعدة الأطباء في تشخيص الالتهاب الرئوي**

⭐ إذا أعجبك المشروع، لا تنسَ نجمة على GitHub!

</div>
