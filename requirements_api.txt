# ===========================================
# Pneumonia Detection API - Dependencies (Optimized)
# ===========================================
# Minimal dependencies for API serving only

# Core ML Framework (CPU-only for smaller image size)
tensorflow-cpu==2.18.0
numpy>=1.26.0,<2.0.0
pillow>=10.0.0

# API Framework  
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17

# Environment Variables
python-dotenv>=1.0.0
